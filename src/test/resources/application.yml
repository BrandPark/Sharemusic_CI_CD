spring:
  profiles:
    active: test
  jpa:
    properties:
      hibernate:
        jdbc.batch_size: ${batchSize}

decorator:
  datasource:
    p6spy:
      enable-logging: false

batchSize: 500
jwt:
  secret: test
app:
  host: http://localhost:8080
