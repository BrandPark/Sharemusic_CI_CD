spring:
  profiles:
    active: test
  jpa:
    properties:
      hibernate:
        jdbc.batch_size: ${batchSize}
        format_sql: true
        use_sql_comments: true

decorator:
  datasource:
    p6spy:
      enable-logging: false

batchSize: 500
